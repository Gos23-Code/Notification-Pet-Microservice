NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

GOOGLE_CLOUD_PROJECT=
# Suscripciones de Pub/Sub a escuchar, separadas por coma (sin espacios).
PUBSUB_SUBSCRIPTIONS=

# Solo hace falta fuera de GCP (local o Docker): ruta DENTRO del contenedor
# al JSON de la service account. En Cloud Run/GKE no se define, se usan las
# credenciales por defecto del entorno (ADC).
GOOGLE_APPLICATION_CREDENTIALS=
